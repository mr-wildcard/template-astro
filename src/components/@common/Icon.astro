---
import { Icon } from "astro-icon/components";

export interface Props {
  name: "" | "??";
  width: number;
  height: number;
}

const { name, width, height, ...props } = Astro.props;
---

<Icon aria-hidden="true" width={width} height={height} name={name} {...props} />
